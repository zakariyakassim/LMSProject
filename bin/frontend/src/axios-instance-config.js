import axios from "axios";

i/*mport * as tunnel from 'tunnel';
const agent = tunnel.httpsOverHttp({
    proxy: {
        host: '172.17.25.173',
        port: 8081,
    },
});

const apiconnection = axios.create({
    httpAgent: agent,
    proxy: true,
});

export default apiconnection
 */
